services:
  # 1. Your Custom Python Bot Service (Forced Free Tier)
  - type: web
    name: ekkshunya-msg-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: BRAVE_API_KEY
        sync: false
      - key: TELEGRAM_TOKEN
        sync: false
      - key: TELEGRAM_CHAT_ID
        sync: false

  # 2. The Core Background WhatsApp Engine (Forced Free Tier)
  - type: web
    name: waha-whatsapp-engine
    env: docker
    plan: free
    dockerfilePath: Dockerfile
